# VibeNest Project Payments disposable E2E

Timestamped QA repository for the Phase 1 simulator and exact-SHA deployment test.
It contains no provider credentials and is deleted after the run.

The baseline app is intentionally payment-free. It already has a narrow server-side QA
buyer identity middleware so the coding agent can integrate checkout and portal endpoints
without inventing a client-authored entitlement identity.
